%h2 Details about: #{@movie.title}

%ul#details
  - if @movie.image.attached?
    = image_tag url_for(@movie.image), alt: "Movie Image", class: "movie-image"
  %li
    Rating:
    = @movie.rating
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")
  %li
    Description:
    = @movie.description
  %li
    Stars:
    = @movie.average_stars.round(2)
  %li
    Rated by:
    = pluralize(@movie.stars.size, "humans") # Ajuste conforme o método correto no modelo

%br
= link_to 'Back to movie list', movies_path, class: 'button'
%br
= link_to 'Edit', edit_movie_path(@movie.id), class: 'button'
